package tuesday;

public class L4_Arrays {
	 public static void main(String[] args) {
		 /*
			 * We will mae an integer array
			 * arrays are vairaibles that can hold a number of things
			 */
			
			//this means the variable is an array
			//int[] intArray; //intArray is an object that can hold things 
			
			int [] intArray = null; //same as int[] intArray // must do this to get it a space in memory (null address)
			
			intArray = new int [10]; //creates a new integer array object of length 10
		
			//can hold 10 char characters 
			

			System.out.println(intArray.length); //null pointer exception 
			//is 10 letters 
			//index 9 
		 
	 }
}
